/**
 * JPA entities mapping database tables (Portuguese column names via @Column).
 * Entities hold no business logic and are never exposed to the API client.
 */
package com.itel.fititel.domain.entity;
