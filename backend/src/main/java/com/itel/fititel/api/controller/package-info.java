/**
 * REST controllers under /api/v1. Controllers validate DTOs with @Valid,
 * delegate to services through mappers and never touch repositories directly.
 */
package com.itel.fititel.api.controller;
